filebeat:
  prospectors:
    -
      input_type: stdin
      fields:
        server: game1
    -
      paths:
        - "*.log"
      fields:
        server: game2

  spool_size: 2048
  idle_timeout: 5s

output:
  logstash:
    hosts: ["localhost"]
    index: hope
